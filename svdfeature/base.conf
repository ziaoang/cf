# example config for Basic Matrix Fatocirzation

# input and output
buffer_feature      = "train.doc2vec.shuffle.buffer"
test:buffer_feature = "test.doc2vec.buffer"
model_out_folder    = "data"

# the global constant bias for prediction
base_score = 3.600280

# number of each kind of features
#num_user   = 6090
#num_item   = 3933
#num_user   = 6040
#num_item   = 3883
num_user   = 6140
num_item   = 3983
num_global = 0

# learning rate for SGD
learning_rate = 0.005

# regularization constant for factor usually denote \lambda in CFx papers
wd_item       = 0.004
wd_user       = 0.004

# number of factor
num_factor = 64

# translation function for output, 0:linear 2:sigmoid
active_type = 0

